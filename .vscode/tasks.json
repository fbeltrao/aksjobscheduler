{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_dotnet_worker",
            "command": "dotnet build",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/workers/dotnet"
            },
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": "$msCompile"
        },
        {
            "label": "build_dotnet_finalizer",
            "command": "dotnet build",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/finalizers/dotnet"
            },
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": "$msCompile"
        }
    ]
}